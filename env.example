# Server port (Render sets PORT automatically)
PORT=8080

# Comma-separated allowed origins for WebSocket/HTTP origin checks
# Example: https://your-frontend.com,https://staging.your-frontend.com
ALLOWED_ORIGIN=

# Requeue the partner when the other user leaves (true|false)
REQUEUE_ON_PARTNER_LEAVE=true

# Redis-compatible connection string for horizontal scaling (Render Key Value / Valkey)
# Example (TLS): rediss://default:password@host:6379
# Example (non-TLS): redis://host:6379
REDIS_URL=

# Optional instance identifier for logs/observability
INSTANCE_ID=

# Per-connection inbound message rate limit (messages per second)
MAX_MSG_PER_SECOND=40
